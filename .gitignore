# Nix build results
result
result-*

# Secrets - SOPS encrypted files are SAFE to commit
# Only block unencrypted secret files
*.secret
*accounts.secret*
!*.secret.template

# SOPS configuration and encrypted files are tracked
# .sops.yaml - defines encryption keys (public keys are safe)
# secrets/*.yaml - encrypted with SOPS (safe to commit)

# Environment files
.env
.env.*

# Personal overrides (if you want to keep local-only configs)
local.nix
secrets.nix

# macOS
.DS_Store

# Editor files
.vscode/
.idea/
*.swp
*.swo
*~

# Claude Code local settings
.claude/settings.local.json

# Backup files
*.backup
*.bak
*.old
