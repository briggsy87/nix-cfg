# Ranger configuration for image previews

# Enable image previews
set preview_images true

# Image preview method
# Options: w3m, iterm2, terminology, urxvt, urxvt-full, kitty, ueberzug
# For Wayland terminals (Alacritty/Ghostty), use ueberzug
set preview_images_method ueberzug

# Use chafa as fallback (works everywhere but lower quality)
# set preview_images_method chafa

# Use the external preview script
set use_preview_script true
set preview_script ~/.config/ranger/scope.sh

# File preview settings
set preview_files true
set preview_directories true
set collapse_preview true

# VCS integration
set vcs_aware true
set vcs_backend_git enabled

# Show hidden files
set show_hidden false

# Which viewmode should be used?
set viewmode miller

# Display git info in status bar
set vcs_aware true
